// const Joi = require('joi');
const { Joi } = require('express-validation')

module.exports = {
    // POST /v1/transaction/api
    // api: {
    //     body: Joi.object({
    //         address: Joi.string().required(),
    //         user_id :Joi.string().required(),
    //         role : Joi.string().required(),
    //     }),
    // },
}
